import { SplashConfig } from './../types/splashtypes';
import { colors } from './../constants/theme';

export const splashconstants: SplashConfig = {
  logoName: 'bus',
  backgroundColor: colors.primary,
  duration: 2000,
};